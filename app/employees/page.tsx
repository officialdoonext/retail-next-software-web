import EmptyPagePlaceholder from "@/components/EmptyPagePlaceholder";

export default function EmployeesPage() {
  return (
    <EmptyPagePlaceholder
      title="Employees"
      description="Manage staff members, roles, permissions, and shifts"
      iconName="employees"
    />
  );
}
